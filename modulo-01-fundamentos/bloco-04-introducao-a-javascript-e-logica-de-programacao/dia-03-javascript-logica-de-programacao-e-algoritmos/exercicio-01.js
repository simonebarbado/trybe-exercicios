 var n = 5;

// código para gerar as linhas
let linha = "";
let symbol = "*";

for (index = 0; index <= n; index += 1) {
    linha += symbol;
}

// código para imprimir as linhas
for (index = 0; index <= n; index += 1) {
    console.log(linha);
}